#include <iostream>
#include <algorithm>
#include <cmath>
#include <string>
#include <cstring>
using namespace std;

void solve(int* arr, int size)
{
    
}

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(0); cout.tie(0);

    int n;
    cin >> n;

    int arr[2002];
    for(int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }

    solve(arr, n);

    return 0;
}